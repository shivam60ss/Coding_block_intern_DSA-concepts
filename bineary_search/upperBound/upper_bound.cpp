//using brute force

#include<bits/stdc++.h>
using namespace std;

int Upper_bound(vector<int>& arr,int x){
    int n=arr.size();
    int lo=0,hi=n-1;
    int ind=-1;
    while(lo<=hi){
        int mid = (lo+hi)/h2;
        if(arr[mid]>x){


        }
    }
}
int main(){
    int n;
    vector<int>arr(n);
    int x;
    cin>>x;
    
int ind2=upper_bound(arr.begin(),arr.end(),x)-arr.begin();
cout<<ind2<<endl;
return 0;
}
